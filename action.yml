name: 'Update Index'
description: 'Update index file according to newly created article'
inputs:
  paths:  # id of input
    description: 'Paths to changed articles'
    required: true
    default: './'
runs:
  using: 'node12'
  main: 'index.js'

  # outputs:
  # time: # id of output
  #   description: 'The time generation is done'